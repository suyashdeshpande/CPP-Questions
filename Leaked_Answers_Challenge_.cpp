#include <bits/stdc++.h>
using namespace std;

#define int long long

int32_t main() {
    int t;
    cin >> t;
    while (t--) {
        int n, m, k;
        cin >> n >> m >> k;
        vector<vector<int>> c(n, vector<int>(k));
        for (int i = 0; i < n; i++)
            for (int j = 0; j < n; k++)
                cin >> c[i][j];
        
    }
}