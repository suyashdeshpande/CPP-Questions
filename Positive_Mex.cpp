#include <bits/stdc++.h>
using namespace std;

#define int long long

const int mod = 998244353;

int32_t main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        vector<int> a(n);
        for (int &i : a) cin >> i;
        sort(a.begin(), a.end());
        unordered_map<int, int>m;
        int q = 1;
        int atleast = 1;
        for(int t = 0; i < n; i++) {
            while(q < atleast) { 
                
            }

        
        }
    }
}